# 1
```
✅含义：

docker wait 命令用于阻塞并等待一个容器的退出
运行这个命令时，它会返回该容器的退出状态码。
这在自动化脚本中非常有用，可以在容器完成其任务后采取进一步的操作


✅用法
docker wait <container_id>

主要用途
获取退出状态：可以获取容器退出时的状态码，以便进行错误处理。
脚本自动化：在编写自动化脚本时，可以确保在容器完成之前不会继续执行后续命令
```
